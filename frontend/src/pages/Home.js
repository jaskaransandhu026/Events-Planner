import PageContent from "../components/PageContent";
const HomePage = () => {
    return (
        <PageContent title='Event Planner'>
            <p>Browse all the events currently going on!</p>
        </PageContent>
    );
}

export default HomePage;

// havent covered defer(), go through it if needed